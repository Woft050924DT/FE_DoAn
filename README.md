
  # Build ReactJS Interface

  This is a code bundle for Build ReactJS Interface. The original project is available at https://www.figma.com/design/hfbVXNIpRlYvASvpVmuNVl/Build-ReactJS-Interface.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  